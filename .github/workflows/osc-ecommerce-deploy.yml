name: 🚀 Deploy osc-ecommerce
on:
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review, closed]
permissions:
  actions: write
  contents: read

jobs:
  pre-check:
    name: check if workflow should run
    runs-on: ubuntu-latest
    steps:
      - uses: dorny/paths-filter@v2
        id: changes
        with:
          filters: |
            composite-actions:
              - '.github/actions/**'
            osc-ui:
              - 'packages/osc-ui/**'
            osc-api-auth: 
              - 'packages/osc-api-auth/**'
            osc-ecommerce:
              - 'packages/osc-ecommerce/**'
            Dockerfiles:
              - 'Dockerfiles/**'
            osc-ecommerce-deploy:
              - '.github/workflows/osc-ecommerce-deploy.yml'
            osc-ecommerce-destroy-apps:
              - '.github/workflows/osc-ecommerce-destroy-apps.yml'
            osc-ecommerce-pr:
              - '.github/workflows/osc-ecommerce-pr-branches.yml'

      - name: ⬇️ Checkout repo
        uses: actions/checkout@v3

        # if the target branch is not equal to main
        # or if the pull request has not been merged
        # or the pr has no file changes
        # or the pr is in draft mode
        # then cancel it
      - if: ${{ github.base_ref != 'main' || github.event.pull_request.merged == false || steps.changes.outputs.changes == '[]' || github.event.pull_request.draft == true }}
        name: Cancel the self CI workflow run
        run: ./workflow-scripts/cancel.sh
  lint:
    name: ⬣ ESLint
    runs-on: ubuntu-latest
    steps:
      - name: 🛑 Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.9.1

      - name: ⬇️ Checkout repo
        uses: actions/checkout@v3

      - name: ⎔ Setup node
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: 📥 Download deps
        uses: bahmutov/npm-install@v1
        with:
          useLockFile: false

      - name: 🔬 Lint
        run: npm run lint

  typecheck:
    name: ʦ TypeScript
    runs-on: ubuntu-latest
    steps:
      - name: 🛑 Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.9.1

      - name: ⬇️ Checkout repo
        uses: actions/checkout@v3

      - name: ⎔ Setup node
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: 📥 Download deps
        uses: bahmutov/npm-install@v1
        with:
          useLockFile: false

      - name: 🔎 Type check
        run: npm run typecheck --if-present

  vitest:
    name: ⚡ Vitest
    env:
      SANITY_STUDIO_API_DATASET: ${{secrets.SANITY_STUDIO_API_DATASET}}
      SANITY_STUDIO_API_PROJECT_ID: ${{secrets.SANITY_STUDIO_API_PROJECT_ID}}
      SANITY_STUDIO_API_TOKEN: ${{secrets.SANITY_STUDIO_API_TOKEN}}
      HUBSPOT_ACCESS_TOKEN: ${{secrets.HUBSPOT_ACCESS_TOKEN}}
      HUBSPOT_PORTAL_ID: ${{secrets.HUBSPOT_PORTAL_ID}}
    runs-on: ubuntu-latest
    steps:
      - name: 🛑 Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.9.1

      - name: ⬇️ Checkout repo
        uses: actions/checkout@v3

      - name: ⎔ Setup node
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - run: npm run setup
      - run: npm run build:osc-ui

      - name: 📥 Download deps
        uses: bahmutov/npm-install@v1
        with:
          useLockFile: false

      - name: ⚡ Run vitest
        run: npm run test

  # // todo - ak - replace pscale db url with new name, playwright related
  playwright:
    name: 🎭 Playwright
    env:
      VAPID_PUBLIC_KEY: ${{ secrets.VAPID_PUBLIC_KEY }}
      VAPID_PRIVATE_KEY: ${{ secrets.VAPID_PRIVATE_KEY }}
      SANITY_STUDIO_API_DATASET: ${{secrets.SANITY_STUDIO_API_DATASET}}
      SANITY_STUDIO_API_PROJECT_ID: ${{secrets.SANITY_STUDIO_API_PROJECT_ID}}
      SANITY_STUDIO_API_TOKEN: ${{secrets.SANITY_STUDIO_API_TOKEN}}
      PLANETSCALE_PRISMA_DATABASE_URL: ${{ secrets.PLANETSCALE_PRISMA_DATABASE_URL_PLAYWRIGHT }}
      SESSION_SECRET: super-duper-secret
      HUBSPOT_ACCESS_TOKEN: ${{secrets.HUBSPOT_ACCESS_TOKEN}}
      HUBSPOT_PORTAL_ID: ${{secrets.HUBSPOT_PORTAL_ID}}
    runs-on: ubuntu-latest
    steps:
      - name: 🛑 Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.9.1

      - name: ⬇️ Checkout repo
        uses: actions/checkout@v3

      # from here
      - name: setupBuild
        uses: ./.github/actions/setupBuild
        id: setupBuild

      - name: movePackageToRoot
        uses: ./.github/actions/movePackageToRoot
        id: movePackageToRoot
        with:
          name: "osc-ecommerce"
      # to here

      - name: 📥 Download deps
        uses: bahmutov/npm-install@v1
        with:
          useLockFile: false

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: ⚙️ Build
        run: npm run build

      - name: Prisma Generate
        run: npx prisma generate

      - name: 🏃‍♂️ Playwright run
        run: npm run test:e2e:run

      - name: Report
        uses: actions/upload-artifact@v2
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

  build:
    name: 🐳 Build
    needs: pre-check
    runs-on: ubuntu-latest
    steps:
      - name: 🛑 Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.9.1

      - name: ⬇️ Checkout repo
        uses: actions/checkout@v3

      - name: setupBuild
        uses: ./.github/actions/setupBuild
        id: setupBuild

      - name: movePackageToRoot
        uses: ./.github/actions/movePackageToRoot
        id: movePackageToRoot
        with:
          name: "osc-ecommerce"

      - name: readAndLogAppNameAndBranch
        uses: ./.github/actions/readAndLogAppNameAndBranch
        id: readAndLogAppNameAndBranch
      - name: 👀 Read app name
        uses: SebRollen/toml-action@v1.0.0
        id: app_name
        with:
          file: "fly.toml"
          field: "app"

      - name: dockerBuild
        uses: ./.github/actions/dockerBuild
        id: dockerBuild
        with:
          PASSWORD_NAME: ${{ steps.readAndLogAppNameAndBranch.outputs.app-name }}
          APP_NAME: ${{ steps.readAndLogAppNameAndBranch.outputs.app-name }}
          PLANETSCALE_SERVICE_TOKEN_ID: ${{ secrets.PLANETSCALE_SERVICE_TOKEN_ID }}
          PLANETSCALE_SERVICE_TOKEN: ${{ secrets.PLANETSCALE_SERVICE_TOKEN }}
          ORG_NAME: ${{ secrets.ORG_NAME }}
          DB_NAME: ${{ secrets.DB_NAME }}
          BRANCH_NAME: main
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
          VAPID_PUBLIC_KEY: ${{ secrets.VAPID_PUBLIC_KEY}}
          VAPID_PRIVATE_KEY: ${{ secrets.VAPID_PRIVATE_KEY}}
          SANITY_STUDIO_API_DATASET: ${{secrets.SANITY_STUDIO_API_DATASET}}
          SANITY_STUDIO_API_PROJECT_ID: ${{secrets.SANITY_STUDIO_API_PROJECT_ID}}
          SANITY_STUDIO_API_TOKEN: ${{secrets.SANITY_STUDIO_API_TOKEN}}
          HUBSPOT_ACCESS_TOKEN: ${{secrets.HUBSPOT_ACCESS_TOKEN}}
          HUBSPOT_PORTAL_ID: ${{secrets.HUBSPOT_PORTAL_ID}}

  deploy:
    name: 🚀 Deploy
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - name: 🛑 Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.9.1

      - name: ⬇️ Checkout repo
        uses: actions/checkout@v3

      - name: movePackageToRoot
        uses: ./.github/actions/movePackageToRoot
        id: movePackageToRoot
        with:
          name: "osc-ecommerce"

      - name: readAndLogAppNameAndBranch
        uses: ./.github/actions/readAndLogAppNameAndBranch
        id: readAndLogAppNameAndBranch

      - name: deployment
        uses: ./.github/actions/deployment
        id: deployment
        with:
          APP_NAME: ${{ steps.readAndLogAppNameAndBranch.outputs.app-name }}
          FLY_PR_BRANCH_SESSION_SECRET: ${{ secrets.FLY_PR_BRANCH_SESSION_SECRET}}
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN}}
          PLANETSCALE_SERVICE_TOKEN_ID: ${{ secrets.PLANETSCALE_SERVICE_TOKEN_ID }}
          PLANETSCALE_SERVICE_TOKEN: ${{ secrets.PLANETSCALE_SERVICE_TOKEN }}
          ORG_NAME: ${{ secrets.ORG_NAME }}
          DB_NAME: ${{ secrets.DB_NAME }}
